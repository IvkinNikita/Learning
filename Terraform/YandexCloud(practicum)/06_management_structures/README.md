## Управляющие структуры в Terraform

В отличие от традиционных императивных языков программирования, Terraform использует **декларативный** язык конфигурации (HCL — HashiCorp Configuration Language), поэтому классические управляющие структуры, такие как `if`, `for`, `while`, отсутствуют в привычном виде. Однако Terraform предоставляет **условные выражения**, **конструкции для итераций** и **механизмы явного управления зависимостями**, которые позволяют выразить сложную логику развертывания.

---

### 1. Условные выражения (`if`-подобная логика)

Terraform поддерживает **тернарный оператор**, который позволяет выбирать значение на основе условия:

```hcl
variable "env" {
  type = string
}

locals {
  instance_type = var.env == "prod" ? "m5.2xlarge" : "t3.micro"
}
```

Это выражение читается как:  
*«Если `var.env` равно `"prod"`, то использовать `"m5.2xlarge"`, иначе — `"t3.micro"`».*

> ⚠️ **Важно**: Terraform вычисляет **обе ветви** тернарного оператора на этапе планирования, поэтому обе должны быть валидными. Например, нельзя использовать `null` в контексте, где ожидается строка, даже если ветвь не будет выбрана.

---

### 2. Циклы (`for`-подобная логика)

#### a) `for_each` — итерация по коллекциям

Используется для создания **множества ресурсов или модулей** на основе списка, множества или карты:

```hcl
variable "subnets" {
  type = list(string)
  default = ["10.0.1.0/24", "10.0.2.0/24", "10.0.3.0/24"]
}

resource "aws_subnet" "example" {
  for_each = toset(var.subnets)

  cidr_block = each.value
  vpc_id     = aws_vpc.main.id
}
```

Ключевые моменты:
- `for_each` принимает **множество (`set`) или карту (`map`)**.
- Каждый элемент становится отдельным экземпляром ресурса.
- Адресация: `aws_subnet.example["10.0.1.0/24"]`.

#### b) `count` — итерация по индексу

Альтернатива `for_each`, работающая с целым числом:

```hcl
variable "instance_count" {
  default = 3
}

resource "aws_instance" "web" {
  count = var.instance_count

  ami           = "ami-12345678"
  instance_type = "t3.micro"
  tags = {
    Name = "web-${count.index + 1}"
  }
}
```

Различия:
- `count` использует **индексы (`0`, `1`, `2`, …)**.
- Изменение количества может привести к **пересозданию ресурсов** (например, при удалении среднего элемента).
- `for_each` устойчивее к изменениям в коллекции, особенно при работе с картами.

#### c) Выражения `for` — преобразование коллекций

Используются **внутри выражений** для создания новых списков или карт:

```hcl
locals {
  names = ["alice", "bob", "charlie"]
  upper_names = [for name in local.names : upper(name)]
  name_to_id  = { for name in local.names : name => substr(sha1(name), 0, 8) }
}
```

Синтаксис:
- `[for x in list : expression]` → список
- `{ for k, v in map : k => expression }` → карта
- Поддерживает фильтрацию с `if`:
  ```hcl
  [for name in local.names : upper(name) if length(name) > 3]
  ```

---

### 3. Явные зависимости: `depends_on`

По умолчанию Terraform автоматически определяет зависимости между ресурсами на основе **ссылок на атрибуты** (implicit dependencies). Например, если `aws_instance` использует `aws_vpc.main.id`, Terraform знает, что VPC должен быть создан первым.

Однако в некоторых случаях зависимости **не могут быть выявлены автоматически**, например:
- При работе с **side effects**, не выражаемыми через атрибуты (например, настройка прав через внешнюю систему).
- При использовании **ресурсов, не возвращающих идентификатор**, но влияющих на другие ресурсы.
- При необходимости **гарантированного порядка** выполнения операций вне графа ссылок.

Для таких случаев используется мета-аргумент **`depends_on`**:

```hcl
resource "aws_iam_policy" "example" {
  name   = "example-policy"
  policy = jsonencode({ /* ... */ })
}

resource "aws_s3_bucket" "logs" {
  bucket = "my-app-logs-bucket"
}

resource "aws_s3_bucket_policy" "logs" {
  bucket = aws_s3_bucket.logs.id
  policy = jsonencode({ /* ... */ })

  # Явно указываем, что политика зависит от IAM-политики,
  # даже если напрямую не использует её атрибуты
  depends_on = [aws_iam_policy.example]
}
```

#### Когда использовать `depends_on`:
- Только когда **автоматическое определение зависимостей недостаточно**.
- Избегайте чрезмерного использования — это снижает декларативность и усложняет поддержку.
- Не используйте `depends_on` для замены отсутствующих ссылок на атрибуты; старайтесь выразить зависимости через данные.

> 💡 **Рекомендация**: Если вы часто используете `depends_on`, возможно, стоит пересмотреть архитектуру конфигурации или использовать модули для явного связывания компонентов.

---

### 4. Ограничения и рекомендации

- **Нет циклов `while` или `do-while`** — логика должна быть полностью декларативной.
- **Нет вложенных управляющих структур в ресурсах** — вся логика должна быть выражена через переменные, локальные значения или модули.
- **Предпочтение `for_each` перед `count`**: особенно когда идентификаторы ресурсов имеют смысл (например, имена, ID). Это упрощает управление состоянием.
- **Избегайте сложной логики в HCL**: если условий или преобразований становится слишком много, лучше вынести их в **внешний скрипт** (например, через `external` data source) или использовать **модули**.

---

### Пример: комбинированное использование

```hcl
variable "environments" {
  type = map(object({
    enabled = bool
    size    = string
  }))
  default = {
    dev  = { enabled = true,  size = "t3.small" }
    prod = { enabled = true,  size = "m5.large" }
    test = { enabled = false, size = "t3.micro" }
  }
}

resource "aws_instance" "app" {
  for_each = {
    for env, config in var.environments :
    env => config
    if config.enabled
  }

  ami           = "ami-12345678"
  instance_type = each.value.size
  tags = {
    Environment = each.key
  }

  # Допустим, перед запуском нужна предварительная настройка
  depends_on = [null_resource.pre_deploy]
}

resource "null_resource" "pre_deploy" {
  # Например, вызов скрипта подготовки или проверка внешней системы
}
```

Этот пример создаст инстансы **только для включённых окружений** (`dev`, `prod`) и гарантирует, что `null_resource.pre_deploy` завершится до их создания.

---

Управляющие структуры и механизмы зависимостей в Terraform — ключевые инструменты для создания гибких, надёжных и поддерживаемых инфраструктурных конфигураций в рамках декларативной модели.
```
