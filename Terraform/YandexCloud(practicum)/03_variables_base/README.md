## 📦 Переменные в Terraform

Этот проект демонстрирует **работу с переменными** в Terraform — ключевой механизм для создания гибких и переиспользуемых конфигураций.

### Что показано:
- Объявление переменных в `variables.tf`
- Типизация (`string`, `number`, `bool`, `list`, `map` и др.)
- Использование значений по умолчанию
- Передача значений через `terraform.tfvars`
- Безопасная работа с чувствительными данными (`sensitive = true`)

### Пример
```hcl
# variables.tf
variable "folder_id" {
  description = "ID каталога в Yandex Cloud"
  type        = string
}

variable "instance_cores" {
  description = "Количество ядер ВМ"
  type        = number
  default     = 2
}