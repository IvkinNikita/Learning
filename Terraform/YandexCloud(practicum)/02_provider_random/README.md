## 🔀 Использование Random Provider

В этом проекте задействован провайдер [`random`](https://registry.terraform.io/providers/hashicorp/random), чтобы **гарантировать уникальность имён** ресурсов при многократных запусках.

### Что генерируется?
- Уникальный суффикс для имени **S3-бакета** (например: `test-bucket-abc123`).

### Зачем это нужно?
- Имена бакетов в Object Storage **глобально уникальны** в рамках Yandex Cloud.
- Без случайного суффикса повторный `terraform apply` завершится ошибкой, если бакет с таким именем уже существует (даже в другом аккаунте).
- Случайная часть делает развёртывание **идемпотентным и повторяемым**.

### Пример использования
```hcl
resource "random_string" "suffix" {
  length  = 8
  special = false
  upper   = false
}

resource "yandex_storage_bucket" "this" {
  bucket = "test-bucket-${random_string.suffix.result}"
  # ...
}